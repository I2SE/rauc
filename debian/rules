#!/usr/bin/make -f

export DH_OPTIONS=-v
export DH_VERBOSE=1
export DEB_BUILD_OPTIONS=nocheck

%:
	dh $@ --with autoreconf

override_dh_auto_configure:
	dh_auto_configure -- --with-dbuspolicydir=/etc/dbus-1/system.d
